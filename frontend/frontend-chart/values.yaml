# Default values for frontend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: artemmihaylov.gitlab.yandexcloud.net:5050/final-project/momo-store/momo-store-frontend
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.

resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 500m
    memory: 512Mi

environment: unknown